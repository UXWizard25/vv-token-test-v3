// Do not edit directly, this file was auto-generated.
//
// BILD Design System Tokens v1.0.0
// Generated by Style Dictionary v5.1.4
//
// Brand: Sportbild | Context: ColorScheme light
//
// Copyright (c) 2026 Axel Springer Deutschland GmbH
// Proprietary and confidential. All rights reserved.
//
// Documentation: https://github.com/UXWizard25/bild-design-system/blob/main/packages/tokens/docs/ios.md
import SwiftUI

/// Protocol for type-safe color access across Light/Dark modes
public protocol SportbildColorScheme: Sendable {
    /// Brand-colored text for emphasis and highlights (e.g., highlighted keywords, brand callouts, promotional text). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var textColorBrand: Color { get }
    /// Brand text color in light mode, neutral white in dark mode (e.g., subtle brand accents that shouldn't be vibrant in dark). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var textColorBrandLightOnly: Color { get }
    /// Primary text color for static content like body copy and labels (e.g., article text, titles, form labels). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var textColorPrimary: Color { get }
    /// Secondary text color for static supporting content (e.g., captions, timestamps, meta information, descriptions). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var textColorSecondary: Color { get }
    /// Tertiary text color for static de-emphasized content (e.g., placeholder text, helper text, footnotes). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var textColorTertiary: Color { get }
    /// Kicker text color for category labels above headlines on standard surface backgrounds. Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var kickerTextColor: Color { get }
    /// Headline text color for article titles and major headings. Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var headlineTextColor: Color { get }
    /// Text color for content overlaid on images, mode-independent (e.g., image captions, teaser headlines on photos). Behavior: Static — same value in light and dark mode.
    var textColorOnImage: Color { get }
    /// Secondary text on dark solid surfaces, mode-independent. Behavior: Static — same value in light and dark mode.
    var textColorSecondaryOnDark: Color { get }
    /// Primary text on light solid surfaces, mode-independent (e.g., text on light colored backgrounds). Behavior: Static — same value in light and dark mode.
    var textColorPrimaryOnLight: Color { get }
    /// Applied to kicker or meta text on dark colored backgrounds to maintain high legibility. Behavior: Constant — always uses semi-transparent white for consistent readability.
    var kickerTextColorOnDark: Color { get }
    /// Used for kicker text placed on bright or light colored backgrounds. Ensures balanced contrast without harsh visual dominance. Behavior: Constant — remains semi-transparent black across both modes.
    var kickerTextColorOnLight: Color { get }
    /// Primary text on dark solid surfaces, mode-independent (e.g., button labels on dark buttons). Behavior: Static — same value in light and dark mode.
    var textColorPrimaryOnDark: Color { get }
    /// Secondary text on light solid surfaces, mode-independent (e.g., secondary text on light colored backgrounds). Behavior: Static — same value in light and dark mode.
    var textColorSecondaryOnLight: Color { get }
    /// Default text color for interactive/clickable elements in idle state (e.g., text links, navigation items, clickable labels). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var textColorIdle: Color { get }
    /// Text color for disabled elements (e.g., inactive button labels, disabled input text/links). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var textColorDisabled: Color { get }
    /// Text color for interactive/clickable elements on hover (e.g., link hover, navigation item hover). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var textColorHover: Color { get }
    /// Text color for interactive/clickable elements in active or visited state (e.g., visited links, active navigation item, current page). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var textColorActive: Color { get }
    /// Primary static text on inverted/contrast backgrounds (e.g., body text in dark sections, footer copy). Not for interactive elements. Behavior: Inverted — swaps light and dark values. Brand-specific.
    var textColorPrimaryInverse: Color { get }
    /// Secondary static text on inverted/contrast backgrounds (e.g., meta info in dark sections). Not for interactive elements. Behavior: Inverted — swaps light and dark values. Brand-specific.
    var textColorSecondaryInverse: Color { get }
    /// High contrast inverse text for maximum readability on inverted surfaces (e.g., white text on dark buttons). Behavior: Inverted — swaps light and dark values.
    var textColorInverseHighContrast: Color { get }
    /// Success text color for confirmation messages and positive feedback. Behavior: Dynamic — adapts between light and dark mode.
    var textColorSuccess: Color { get }
    /// Error text color for error messages and validation feedback. Behavior: Dynamic — adapts between light and dark mode.
    var textColorError: Color { get }
    /// Warning text color for caution messages and alerts. Behavior: Dynamic — adapts between light and dark mode.
    var textColorWarning: Color { get }
    /// Primary background color for main surfaces and page backgrounds (e.g., page background, primary containers). Behavior: Dynamic — adapts between light and dark mode. Brand-specific in dark mode.
    var bgColorPrimary: Color { get }
    /// Background color for disabled interactive elements (e.g., inactive buttons, disabled inputs). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var bgColorDisabled: Color { get }
    /// Secondary background for elevated containers (e.g., cards on primary background, sidebar panels). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var bgColorSecondary: Color { get }
    /// Dark background that remains constant regardless of color mode (e.g. media containers). Behavior: Static — same value in light and dark mode. Brand-specific.
    var bgColorPrimaryConstDark: Color { get }
    /// Quaternary background for fourth-level elements (e.g., pressed states, nested backgrounds). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var bgColorQuaternary: Color { get }
    /// Tertiary background for third-level containers (e.g., input fields, nested cards, hover states). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var bgColorTertiary: Color { get }
    /// Light background that remains constant regardless of color mode (e.g., fixed white elements). Behavior: Static — same value in light and dark mode. Brand-specific.
    var bgColorPrimaryConstLight: Color { get }
    /// Quintenary background for fifth-level elements and high-contrast utility surfaces. Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var bgColorQuintenary: Color { get }
    /// Brand background in light mode, neutral white in dark mode (e.g., accent elements that shouldn't be vibrant in dark). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var bgColorBrandLightOnly: Color { get }
    /// Solid brand background for primary CTAs and brand-emphasized elements (e.g., primary buttons, badges). Behavior: Static — same value in light and dark mode. Brand-specific.
    var bgColorBrandSolid: Color { get }
    /// Solid brand background for hover interactions (e.g., CTA button hover). Behavior: Static — same value in light and dark mode. Brand-specific.
    var bgColorBrandSolidHover: Color { get }
    /// Solid brand background for active/pressed state (e.g., CTA button pressed). Behavior: Static — same value in light and dark mode. Brand-specific.
    var bgColorBrandSolidActive: Color { get }
    /// Subtle brand tint background for soft brand presence (e.g., notification backgrounds, selected list items). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var bgColorBrandTint: Color { get }
    /// Subtle brand tint background for hover state (e.g., secondary button hover). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var bgColorBrandTintHover: Color { get }
    /// nverted primary background for contrast sections (e.g., dark header on light page, footer). Behavior: Inverted — swaps light and dark values. Brand-specific.
    var bgColorPrimaryInverse: Color { get }
    /// Inverted tertiary background for nested contrast elements (e.g., code blocks, nested menus). Behavior: Inverted — swaps light and dark values. Brand-specific.
    var bgColorTertiaryInverse: Color { get }
    /// Inverted quaternary background for subtle contrast distinctions. Behavior: Inverted — swaps light and dark values. Brand-specific.
    var bgColorQuaternaryInverse: Color { get }
    /// Inverted secondary background for elevated contrast elements (e.g., tooltips, popovers). Behavior: Inverted — swaps light and dark values. Brand-specific.
    var bgColorSecondaryInverse: Color { get }
    /// Inverted quintenary background for high-contrast inverse elements. Behavior: Inverted — swaps light and dark values. Brand-specific.
    var bgColorQuintenaryInverse: Color { get }
    /// Success feedback background for positive states (e.g., success message background, validated field). Behavior: Dynamic — adapts between light and dark mode.
    var bgColorSuccessTint: Color { get }
    /// Solid success background for positive actions (e.g., confirm button, success badge). Behavior: Static — same value in light and dark mode.
    var bgColorSuccessSolid: Color { get }
    /// Solid success background for hover state (e.g., confirm button hover). Behavior: Static — same value in light and dark mode.
    var bgColorSuccessSolidHover: Color { get }
    /// Success feedback background for hover state (e.g., success notification hover). Behavior: Dynamic — adapts between light and dark mode.
    var bgColorSuccessTintHover: Color { get }
    /// Solid success background for active/pressed state (e.g., confirm button pressed). Behavior: Static — same value in light and dark mode.
    var bgColorSuccessSolidActive: Color { get }
    /// Gradient stop color for tertiary background transitions. Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var bgColorTertiaryGradientStop: Color { get }
    /// Gradient stop color for primary background fade effects (e.g., scroll shadows, content fade-outs). Behavior: Dynamic — adapts between light and dark mode.
    var bgColorPrimaryGradientStop: Color { get }
    /// Gradient stop color for secondary background transitions. Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var bgColorSecondaryGradientStop: Color { get }
    /// A semi-transparent black overlay used to dim background content when modals, drawers, or dialogs are active. Identical opacity and tone in both Light and Dark mode to ensure consistent overlay depth.
    var overlayScrimBlackConst: Color { get }
    /// A semi-transparent white overlay used to brighten or fade background layers, often applied behind bottom sheets or temporary panels. Behavior: Constant — same opacity level across modes to preserve uniform layering behavior.
    var overlayScrimWhiteConst: Color { get }
    /// Error feedback background for error states (e.g., error message background, invalid field highlight). Behavior: Dynamic — adapts between light and dark mode.
    var bgColorErrorTint: Color { get }
    /// Error feedback background for hover state (e.g., error notification hover). Behavior: Dynamic — adapts between light and dark mode.
    var bgColorErrorTintHover: Color { get }
    /// Solid error background for critical actions (e.g., delete button, error badge). Behavior: Static — same value in light and dark mode.
    var bgColorErrorSolid: Color { get }
    /// Solid error background for hover state (e.g., destructive button hover). Behavior: Static — same value in light and dark mode.
    var bgColorErrorSolidHover: Color { get }
    /// Warning feedback background for caution states (e.g., warning banner, attention indicators). Behavior: Dynamic — adapts between light and dark mode.
    var bgColorWarningTint: Color { get }
    /// Warning feedback background for hover state. Behavior: Dynamic — adapts between light and dark mode.
    var bgColorWarningTintHover: Color { get }
    /// Solid warning background for caution indicators (e.g., warning badge, alert background). Behavior: Static — same value in light and dark mode.
    var bgColorWarningSolid: Color { get }
    /// Solid warning background for hover state. Behavior: Static — same value in light and dark mode.
    var bgColorWarningSolidHover: Color { get }
    /// Default border color for static containers and separators (e.g., card borders, section dividers). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var borderColorDefault: Color { get }
    /// Subtle low-contrast border for soft static separations (e.g., list separators, subtle dividers). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var borderColorSubtle: Color { get }
    /// Extra strong border for maximum static contrast (e.g., critical sections, strong separations). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var borderColorExtraStrong: Color { get }
    /// Strong border for static emphasis (e.g., highlighted sections, important containers). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var borderColorStrong: Color { get }
    /// Subtle border on dark solid backgrounds (e.g., subtle dividers on dark UI). Behavior: Static — same value in light and dark mode. Brand-specific.
    var borderColorSubtleOnDark: Color { get }
    /// Default border on dark solid backgrounds. Behavior: Static — same value in light and dark mode. Brand-specific.
    var borderColorDefaultOnDark: Color { get }
    /// Subtle border on light solid backgrounds. Behavior: Static — same value in light and dark mode. Brand-specific.
    var borderColorSubtleOnLight: Color { get }
    /// Default border on light solid backgrounds. Behavior: Static — same value in light and dark mode. Brand-specific.
    var borderColorDefaultOnLight: Color { get }
    /// Success border for valid states (e.g., validated input, successful upload). Behavior: Dynamic — adapts between light and dark mode.
    var borderColorSuccess: Color { get }
    /// Error border for invalid states (e.g., form validation error, required field indicator). Behavior: Dynamic — adapts between light and dark mode.
    var borderColorError: Color { get }
    /// Warning border for caution states (e.g., field warning, attention required). Behavior: Dynamic — adapts between light and dark mode.
    var borderColorWarning: Color { get }
    /// Border color for disabled elements (e.g., disabled input borders, inactive containers). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var borderColorDisabled: Color { get }
    /// Border for selected elements (e.g., selected checkbox, active tab indicator). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var borderColorSelected: Color { get }
    /// Border for active elements (e.g., active tab indicator). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var borderColorActive: Color { get }
    /// Default border color for interactive elements in idle state (e.g., input fields, buttons, selectable items). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var borderColorIdle: Color { get }
    /// Default border on inverted surfaces. Behavior: Inverted — swaps light and dark values. Brand-specific.
    var borderColorDefaultInverse: Color { get }
    /// Subtle border on inverted surfaces (e.g., subtle separators in dark sections). Behavior: Inverted — swaps light and dark values. Brand-specific.
    var borderColorSubtleInverse: Color { get }
    /// Brand-colored border for emphasis (e.g., active tabs, selected items, brand highlights). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var borderColorBrand: Color { get }
    /// Brand border in light mode, neutral white in dark mode (e.g., accent borders that shouldn't be vibrant in dark). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var borderColorBrandLightOnly: Color { get }
    /// Brand border that remains constant regardless of color mode. Behavior: Static — same value in light and dark mode. Brand-specific.
    var borderColorBrandConst: Color { get }
    /// Zero-contrast border matching surface color (e.g., invisible borders for layout, spacing helpers). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var borderColorNoContrast: Color { get }
    /// Default icon color for interactive/clickable icons in idle state (e.g., icon buttons, navigation icons, action icons). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var iconColorIdle: Color { get }
    /// Icon color for disabled elements (e.g., inactive button icons, disabled controls). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var iconColorDisabled: Color { get }
    /// Icon color for interactive/clickable icons on hover (e.g., icon button hover, navigation icon hover). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var iconColorHover: Color { get }
    /// Icon color for interactive/clickable icons in active or selected state (e.g., active navigation, selected action). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var iconColorActive: Color { get }
    /// Secondary icon on light solid surfaces. Behavior: Static — same value in light and dark mode.
    var iconColorSecondaryOnLight: Color { get }
    /// Primary icon on dark solid surfaces (e.g., icons on dark buttons, player controls). Behavior: Static — same value in light and dark mode.
    var iconColorPrimaryOnDark: Color { get }
    /// Secondary icon on dark solid surfaces. Behavior: Static — same value in light and dark mode.
    var iconColorSecondaryOnDark: Color { get }
    /// Primary icon on light solid surfaces. Behavior: Static — same value in light and dark mode.
    var iconColorPrimaryOnLight: Color { get }
    /// Icon color for elements overlaid on images (e.g., play button on video thumbnail). Mode-independent. Behavior: Static — same value in light and dark mode.
    var iconColorConstOnImage: Color { get }
    /// Success icon color for success indicators (e.g., checkmark icon, completion indicator). Behavior: Dynamic — adapts between light and dark mode.
    var iconColorSuccess: Color { get }
    /// Error icon color for error indicators (e.g., error badge icon, validation error icon). Behavior: Dynamic — adapts between light and dark mode.
    var iconColorError: Color { get }
    /// Warning icon color for caution indicators (e.g., warning triangle, alert icon). Behavior: Dynamic — adapts between light and dark mode.
    var iconColorWarning: Color { get }
    /// Brand-colored icon for emphasis (e.g., featured icons, brand indicators). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var iconColorBrand: Color { get }
    /// Brand icon color in light mode, neutral white in dark mode. Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var iconColorBrandLightOnly: Color { get }
    /// Primary icon color for static/decorative icons (e.g., section icons, illustrative icons, status indicators). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var iconColorPrimary: Color { get }
    /// Secondary icon color for static supporting icons (e.g., metadata icons, decorative icons, list markers). Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var iconColorSecondary: Color { get }
    /// Tertiary icon color for static de-emphasized icons (e.g., subtle indicators, background icons).  Behavior: Dynamic — adapts between light and dark mode. Brand-specific.
    var iconColorTertiary: Color { get }
    /// Primary icon on inverted/contrast backgrounds. Behavior: Inverted — swaps light and dark values. Brand-specific.
    var iconColorPrimaryInverse: Color { get }
    /// Secondary icon on inverted/contrast backgrounds. Behavior: Inverted — swaps light and dark values. Brand-specific.
    var iconColorSecondaryInverse: Color { get }
    /// High contrast inverse icon for maximum visibility on inverted surfaces. Behavior: Inverted — swaps light and dark values.
    var iconColorInverseHighContrast: Color { get }
    /// Layer opacity value at 5% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity05: Double { get }
    /// Layer opacity value at 10% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity10: Double { get }
    /// Layer opacity value at 20% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity20: Double { get }
    /// Layer opacity value at 30% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity30: Double { get }
    /// Layer opacity value at 40% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity40: Double { get }
    /// Layer opacity value at 50% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity50: Double { get }
    /// Layer opacity value at 60% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity60: Double { get }
    /// Layer opacity value at 70% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity70: Double { get }
    /// Layer opacity value at 80% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity80: Double { get }
    /// Layer opacity value at 90% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity90: Double { get }
    /// Layer opacity value at 100% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity100: Double { get }
    /// Layer opacity value at 00% for transparency effects (e.g., overlays, fade effects). Behavior: Static — same value in light and dark mode.
    var layerOpacity00: Double { get }
}

/// Light mode color implementation
public struct SportbildLightColors: SportbildColorScheme, DesignColorScheme {
    public static let shared = SportbildLightColors()
    private init() {}

    public let textColorBrand: Color = Color(hex: 0x174482)
    public let textColorBrandLightOnly: Color = Color(hex: 0x174482)
    public let textColorPrimary: Color = Color(hex: 0x222628)
    public let textColorSecondary: Color = Color(hex: 0x565A5F)
    public let textColorTertiary: Color = Color(hex: 0x6E787C)
    public let kickerTextColor: Color = Color(hex: 0x6A91CF)
    public let headlineTextColor: Color = Color(hex: 0x0C2F61)
    public let textColorOnImage: Color = Color(hex: 0xFFFFFF)
    public let textColorSecondaryOnDark: Color = Color(hex: 0xDDE1E2)
    public let textColorPrimaryOnLight: Color = Color(hex: 0x222628)
    public let kickerTextColorOnDark: Color = Color(hex: 0xFFFFFF, alpha: 0.80)
    public let kickerTextColorOnLight: Color = Color(hex: 0x000000, alpha: 0.70)
    public let textColorPrimaryOnDark: Color = Color(hex: 0xFFFFFF)
    public let textColorSecondaryOnLight: Color = Color(hex: 0x565A5F)
    public let textColorIdle: Color = Color(hex: 0x345EA0)
    public let textColorDisabled: Color = Color(hex: 0x8FACDB)
    public let textColorHover: Color = Color(hex: 0x0B2647)
    public let textColorActive: Color = Color(hex: 0x0B2647)
    public let textColorPrimaryInverse: Color = Color(hex: 0xD6E1F2)
    public let textColorSecondaryInverse: Color = Color(hex: 0x8FACDB)
    public let textColorInverseHighContrast: Color = Color(hex: 0xFFFFFF)
    public let textColorSuccess: Color = Color(hex: 0x006E3E)
    public let textColorError: Color = Color(hex: 0xC10000)
    public let textColorWarning: Color = Color(hex: 0x9E4401)
    public let bgColorPrimary: Color = Color(hex: 0xFFFFFF)
    public let bgColorDisabled: Color = Color(hex: 0xEFF1F5)
    public let bgColorSecondary: Color = Color(hex: 0xEFF1F5)
    public let bgColorPrimaryConstDark: Color = Color(hex: 0x0B2647)
    public let bgColorQuaternary: Color = Color(hex: 0xC3C9DB)
    public let bgColorTertiary: Color = Color(hex: 0xD6E1F2)
    public let bgColorPrimaryConstLight: Color = Color(hex: 0xFFFFFF)
    public let bgColorQuintenary: Color = Color(hex: 0x6A91CF)
    public let bgColorBrandLightOnly: Color = Color(hex: 0x174482)
    public let bgColorBrandSolid: Color = Color(hex: 0x174482)
    public let bgColorBrandSolidHover: Color = Color(hex: 0x0C2F61)
    public let bgColorBrandSolidActive: Color = Color(hex: 0x091C2E)
    public let bgColorBrandTint: Color = Color(hex: 0xD6E1F2)
    public let bgColorBrandTintHover: Color = Color(hex: 0xC3C9DB)
    public let bgColorPrimaryInverse: Color = Color(hex: 0x0B2647)
    public let bgColorTertiaryInverse: Color = Color(hex: 0x113B72)
    public let bgColorQuaternaryInverse: Color = Color(hex: 0x174482)
    public let bgColorSecondaryInverse: Color = Color(hex: 0x0C2F61)
    public let bgColorQuintenaryInverse: Color = Color(hex: 0x6A91CF)
    public let bgColorSuccessTint: Color = Color(hex: 0xDEF7E6)
    public let bgColorSuccessSolid: Color = Color(hex: 0x158751)
    public let bgColorSuccessSolidHover: Color = Color(hex: 0x006E3E)
    public let bgColorSuccessTintHover: Color = Color(hex: 0xB9EFCD)
    public let bgColorSuccessSolidActive: Color = Color(hex: 0x0D5232)
    public let bgColorTertiaryGradientStop: Color = Color(hex: 0xD6E1F2, alpha: 0.00)
    public let bgColorPrimaryGradientStop: Color = Color(hex: 0xFFFFFF, alpha: 0.00)
    public let bgColorSecondaryGradientStop: Color = Color(hex: 0xEFF1F5, alpha: 0.00)
    public let overlayScrimBlackConst: Color = Color(hex: 0x000000, alpha: 0.70)
    public let overlayScrimWhiteConst: Color = Color(hex: 0xFFFFFF, alpha: 0.70)
    public let bgColorErrorTint: Color = Color(hex: 0xFFECEC)
    public let bgColorErrorTintHover: Color = Color(hex: 0xFFDADA)
    public let bgColorErrorSolid: Color = Color(hex: 0xC10000)
    public let bgColorErrorSolidHover: Color = Color(hex: 0x930000)
    public let bgColorWarningTint: Color = Color(hex: 0xFFEDDF)
    public let bgColorWarningTintHover: Color = Color(hex: 0xFEDBC1)
    public let bgColorWarningSolid: Color = Color(hex: 0xFD8227)
    public let bgColorWarningSolidHover: Color = Color(hex: 0xEF6702)
    public let borderColorDefault: Color = Color(hex: 0xC3C9DB)
    public let borderColorSubtle: Color = Color(hex: 0xD6E1F2)
    public let borderColorExtraStrong: Color = Color(hex: 0x0B2647)
    public let borderColorStrong: Color = Color(hex: 0x4676C3)
    public let borderColorSubtleOnDark: Color = Color(hex: 0x113B72)
    public let borderColorDefaultOnDark: Color = Color(hex: 0x345EA0)
    public let borderColorSubtleOnLight: Color = Color(hex: 0xD6E1F2)
    public let borderColorDefaultOnLight: Color = Color(hex: 0xC3C9DB)
    public let borderColorSuccess: Color = Color(hex: 0x006E3E)
    public let borderColorError: Color = Color(hex: 0xDD0000)
    public let borderColorWarning: Color = Color(hex: 0xC65502)
    public let borderColorDisabled: Color = Color(hex: 0xD6E1F2)
    public let borderColorSelected: Color = Color(hex: 0x174482)
    public let borderColorActive: Color = Color(hex: 0x174482)
    public let borderColorIdle: Color = Color(hex: 0x6A91CF)
    public let borderColorDefaultInverse: Color = Color(hex: 0x345EA0)
    public let borderColorSubtleInverse: Color = Color(hex: 0x113B72)
    public let borderColorBrand: Color = Color(hex: 0x174482)
    public let borderColorBrandLightOnly: Color = Color(hex: 0x174482)
    public let borderColorBrandConst: Color = Color(hex: 0x174482)
    public let borderColorNoContrast: Color = Color(hex: 0xFFFFFF)
    public let iconColorIdle: Color = Color(hex: 0x345EA0)
    public let iconColorDisabled: Color = Color(hex: 0x8FACDB)
    public let iconColorHover: Color = Color(hex: 0x0B2647)
    public let iconColorActive: Color = Color(hex: 0x0B2647)
    public let iconColorSecondaryOnLight: Color = Color(hex: 0x565A5F)
    public let iconColorPrimaryOnDark: Color = Color(hex: 0xFFFFFF)
    public let iconColorSecondaryOnDark: Color = Color(hex: 0xDDE1E2)
    public let iconColorPrimaryOnLight: Color = Color(hex: 0x222628)
    public let iconColorConstOnImage: Color = Color(hex: 0xFFFFFF)
    public let iconColorSuccess: Color = Color(hex: 0x006E3E)
    public let iconColorError: Color = Color(hex: 0xC10000)
    public let iconColorWarning: Color = Color(hex: 0x9E4401)
    public let iconColorBrand: Color = Color(hex: 0x174482)
    public let iconColorBrandLightOnly: Color = Color(hex: 0x174482)
    public let iconColorPrimary: Color = Color(hex: 0x222628)
    public let iconColorSecondary: Color = Color(hex: 0x565A5F)
    public let iconColorTertiary: Color = Color(hex: 0x6E787C)
    public let iconColorPrimaryInverse: Color = Color(hex: 0xD6E1F2)
    public let iconColorSecondaryInverse: Color = Color(hex: 0x8FACDB)
    public let iconColorInverseHighContrast: Color = Color(hex: 0xFFFFFF)
    public let layerOpacity05: Double = 0.05
    public let layerOpacity10: Double = 0.1
    public let layerOpacity20: Double = 0.2
    public let layerOpacity30: Double = 0.3
    public let layerOpacity40: Double = 0.4
    public let layerOpacity50: Double = 0.5
    public let layerOpacity60: Double = 0.6
    public let layerOpacity70: Double = 0.7
    public let layerOpacity80: Double = 0.8
    public let layerOpacity90: Double = 0.9
    public let layerOpacity100: Double = 1
    public let layerOpacity00: Double = 0
}
