# Project-wide Gradle settings for BILD Design System Icons

# Android settings
android.useAndroidX=true
android.nonTransitiveRClass=true

# Kotlin settings
kotlin.code.style=official

# Build optimization
org.gradle.parallel=true
org.gradle.caching=true
org.gradle.configureondemand=true

# Default version (overridden by CI/CD via -PVERSION=x.x.x)
# Keep in sync with packages/icons/svg/package.json
VERSION=1.0.4
